"""Hostel Agent: hostel applications, allocation, rules, rooms, mess, fees, complaints."""
from app.agents.base_agent import DepartmentAgent

hostel_agent = DepartmentAgent(department="Hostel", agent_name="Hostel Agent")
