"""Academic Agent: courses, subjects, attendance, examinations, syllabus, academic rules."""
from app.agents.base_agent import DepartmentAgent

academic_agent = DepartmentAgent(department="Academic", agent_name="Academic Agent")
